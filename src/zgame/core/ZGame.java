package zgame.core;

/**
 * A convenience class that extends {@link Game} that doesn't use a special data type parameters
 * Basically, extend this instead of Game if you don't want to deal with as many extra Type parameters
 */
public class ZGame extends Game<Object>{
	
}
